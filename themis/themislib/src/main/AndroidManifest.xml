<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          
    package="themis.heka.house.themislib"
    >

    <application android:label="@string/app_name"
        android:allowBackup="true"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:name="com.orm.SugarApp">
        <meta-data android:name="DATABASE" android:value="asteria.db" />
        <meta-data android:name="VERSION" android:value="6" />
        <meta-data android:name="QUERY_LOG" android:value="true" />
        <meta-data android:name="DOMAIN_PACKAGE_NAME" android:value="house.heka.local.models" />

    </application>

</manifest>
